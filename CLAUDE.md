# FlowLedger 投记 · 个人投资记账

## 项目概述
个人全渠道投资出入金管理工具，支持大A、美股、港股、加密货币、基金、黄金、期货等多资产类型的资金记录、盈亏统计、行为分析与复盘建议。单文件 HTML 应用，可静态部署。

## 技术栈
- **单文件 `index.html`**（约 2000 行），无需构建工具
- Vue 3（CDN）、SheetJS/xlsx（CDN）、Day.js（CDN）、Chart.js（CDN）
- 数据存储：浏览器 localStorage
- 云同步：GitHub Gist API（Bearer Token 认证，免费、带版本历史）
- PWA：Service Worker + Manifest，支持离线使用和添加到主屏幕
- 深色/浅色金融风 UI，桌面端宽布局 + 手机端底部 Tab

## 文件结构
```
FlowLedger/
├── index.html      # 主应用（前端全部代码）
├── sw.js           # Service Worker（离线缓存）
├── manifest.json   # PWA 配置
├── icon-192.png    # PWA 图标 192x192
├── icon-512.png    # PWA 图标 512x512
├── icon.svg        # 图标源文件
├── deploy.sh       # 阿里云一键部署脚本（含 nginx 配置 + Node.js 同步服务，已废弃）
├── CLAUDE.md       # 本文件
└── README.md       # 项目介绍
```

## 已实现功能

### 核心功能
- [x] 出入金记录 CRUD（桌面端表格内直接编辑，移动端卡片+Modal）
- [x] 快速录入（复制上一笔、平台按资产类型预设建议、货币汇率自动获取）
- [x] 冷静提醒（入金时检测频率、金额异常、预算超支，弹窗确认）
- [x] 盈亏规则：出金总额 > 入金总额 → 盈利（自动换算多币种为 CNY）
- [x] 预置 17 条演示数据（首次打开即有内容）
- [x] 多币种支持：CNY、USD、HKD、USDT，自动汇率换算

### 资产类型
- [x] 8 种资产大类：大A、美股、港股、加密货币、基金、黄金、期货、其他
- [x] 每种类型预设常用平台（40+ 平台），选择资产类型后自动补全平台建议
- [x] 资产配置饼图（投入分布 / 净值分布切换）
- [x] 按资产类型的盈亏统计

### 统计与分析
- [x] 总览指标：总入金、总出金、净盈亏、回本比例
- [x] 周/月/年盈亏统计（含累计盈亏列）+ Chart.js 趋势折线图
- [x] 账户/平台维度统计
- [x] 周期对比（当前 vs 上期 + 环比变化）
- [x] 大额出入金提醒（阈值可配置）
- [x] 月度预算（进度条 + 超支警告）

### 筛选系统
- [x] 按月份、类型、关键词、日期区间、金额区间、平台多选、资产类型多选
- [x] 命中高亮（蓝色），未命中变淡（不隐藏）
- [x] 筛选结果汇总条（总入金、总出金、净盈亏等）
- [x] 一键定位第一条命中记录

### 投入行为分析
- [x] 心理画像标签：纪律执行、节奏稳健、周末活跃、频繁操作、亏后加仓、大额冲动、持续流入
- [x] 针对性行为建议
- [x] 星期分布柱状图
- [x] 金额区间分布
- [x] 关键指标：平均间隔、密集操作次数、投入标准差、最长连续流入

### 复盘与建议
- [x] 自动生成周期建议（基于可配置的阈值规则）
- [x] 复盘清单（checkbox，按周期存储）
- [x] 周期复盘笔记
- [x] 6 条阈值规则可配置：亏损提醒、强提醒、回收率、入金速度倍数、大额阈值、连续亏损周期

### 数据管理
- [x] Excel 导出（5 个 Sheet：投入/取出/流水/盈亏总览/月度统计）
- [x] Excel 导入（支持多 Sheet 格式 + 中英文列名映射）
- [x] JSON 备份/导入
- [x] 自动快照（最多 10 份 FIFO）+ 手动快照 + 一键恢复
- [x] 向后兼容旧版 localStorage key

### GitHub Gist 云同步
- [x] GitHub Personal Access Token 认证（仅需 gist scope）
- [x] 首次上传自动创建 Private Gist
- [x] 后续同步更新已有 Gist
- [x] 拉取/上传/一键合并同步
- [x] 启动时自动静默拉取最新数据
- [x] 合并策略：本地优先，去重合并
- [x] Gist 天然版本历史，可回溯恢复

### 响应式布局
- [x] 桌面端（>860px）：长页面滚动，统计+建议并排双栏，表格内编辑
- [x] 平板（680-860px）：单栏宽表格
- [x] 手机端（<680px）：底部 Tab 切换 + FAB 浮动按钮 + 弹出式 Modal + 卡片列表

### 其他
- [x] PWA：Service Worker 离线缓存 + Manifest + iOS 主屏幕支持
- [x] 深色/浅色主题切换（自动跟随系统 + 手动）
- [x] 90+ 条交易大师随机名言

## 部署方式
- **本地使用**：直接浏览器打开 index.html
- **GitHub Pages**：直接部署，配合 Gist 同步实现多端数据共享
- **Netlify / Vercel**：直接拖拽 index.html 部署
- **服务器部署**：运行 deploy.sh（需 nginx + Node.js + pm2，已不推荐）

## 数据模型
```javascript
// localStorage key: flowledger_entries_v1
[{
  id,           // UUID (crypto.randomUUID)
  date,         // "YYYY-MM-DD"
  amount,       // 数字（原币种金额）
  rate,         // 汇率（非CNY币种兑CNY），数字或空
  exchange,     // 账户/平台名称
  note,         // 备注 / 投入理由
  type,         // "入金" | "出金"
  assetType,    // 资产大类："大A"|"美股"|"港股"|"加密货币"|"基金"|"黄金"|"期货"|"其他"|""
  currency      // 币种："CNY"|"USD"|"HKD"|"USDT"
}]
```

## 待办 / 可优化项
- [ ] 链上数据自动导入（钱包地址 / 交易所 API 对接）
- [ ] 多语言 i18n（至少中英文）
- [ ] 税务报告导出（部分地区 crypto 需报税）
- [ ] 持仓追踪（不限于出入金流水，扩展到头寸管理）
- [ ] 单元测试 / E2E 测试
- [ ] 数据可视化 Dashboard 自定义
